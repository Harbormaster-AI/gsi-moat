The purpose of this revision is to illustrate the simplicity of using a declarative system to make large feature changes to a system design.  As with other similar declarative systems, the complexities of configuration and instrumentation are handled for the user.  

In this case, the change from mysql to mongodb is a simple as a name change.

This is likewise for such feature changes as blueprint, domain-model, ci/cd, terraform/kubernetes targets, and more.